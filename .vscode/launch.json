{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run CPP_Executable",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/CPP_Executable",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true,
            "MIMode": "gdb",
            "preLaunchTask": "Build CPP_Executable",
            "miDebuggerPath": "/usr/local/bin/gdb" // Specify the path to GDB if necessary
        }
    ]
}
